<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.smartwealth.trade.mapper.TradeLocalMsgMapper">
    <update id="updateStatusSuccess">
        UPDATE t_trade_local_msg
        SET status = 1
        WHERE msg_id = #{msgId}
    </update>
    <!--
        【BUGFIX】同步修复 next_retry IS NULL 漏扫问题，保证新插入的本地消息
                 在 ConfirmCallback 没回执时也能被定时任务补偿。
    -->
    <select id="selectPendingMsgs" resultType="com.smartwealth.trade.entity.TradeLocalMsg">
        SELECT * FROM t_trade_local_msg
        WHERE status = 0
          AND (next_retry IS NULL OR next_retry &lt;= NOW())
          AND (retry_count IS NULL OR retry_count &lt; 5)
        LIMIT 500
    </select>

</mapper>