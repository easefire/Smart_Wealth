<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.smartwealth.trade.mapper.DailyProfitMapper">
    <insert id="insertBatch">
        INSERT INTO t_trade_daily_profit
        (id,order_id, user_id, prod_id, daily_profit, profit_date, type, create_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.id},#{item.orderId}, #{item.userId}, #{item.prodId}, #{item.dailyProfit}, #{item.profitDate}, #{item.type}, NOW())
        </foreach>
    </insert>
</mapper>