<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smartwealth.product.mapper.ProdInfoMapper">

    <select id="selectProductVOList" resultType="com.smartwealth.product.vo.ProductVO">
        select
            id,
            code,
            name,
            cycle,
            base_rate as baseRate,
            latest_rate as latestRate,
            current_nav as currentNav,
            risk_level as riskLevel,
            total_stock-locked_stock as availableStock
        from
            t_prod_info
        where total_stock-locked_stock &gt; 0 and risk_level &lt;= #{riskLevel}
    </select>
</mapper>
